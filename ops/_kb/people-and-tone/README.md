# Scope: people-and-tone

Writing voice, audience-specific tone calibration, communication patterns, and delegation style.

> **Example scope — adapt to your domains.** Rename or repurpose this scope to fit your operation. Keep KB topic files here as `{topic}.md` with the frontmatter shown in `../_kb-lint.py`. The voice guide template lives at `voice.md`. Run `/kb-lint` after adding files.
