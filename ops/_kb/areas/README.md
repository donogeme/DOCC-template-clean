# Scope: areas

Standing operating-area references — direct-report ownership, coverage/OOO standards, OKRs, and recurring program areas.

> **Example scope — adapt to your domains.** Rename or repurpose this scope to fit your operation. Keep KB topic files here as `{topic}.md` with the frontmatter shown in `../_kb-lint.py`. Run `/kb-lint` after adding files.
